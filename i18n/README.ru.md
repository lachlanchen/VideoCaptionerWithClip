[English](../README.md) · [العربية](README.ar.md) · [Español](README.es.md) · [Français](README.fr.md) · [日本語](README.ja.md) · [한국어](README.ko.md) · [Tiếng Việt](README.vi.md) · [中文 (简体)](README.zh-Hans.md) · [中文（繁體）](README.zh-Hant.md) · [Deutsch](README.de.md) · [Русский](README.ru.md)


# Clip-GPT-Captioning

![Python](https://img.shields.io/badge/Python-3.10%2B-blue)
![PyTorch](https://img.shields.io/badge/PyTorch-2.x-red)
![Transformers](https://img.shields.io/badge/HuggingFace-Transformers-yellow)
![Status](https://img.shields.io/badge/README-Expanded-success)
![Repo Layout](https://img.shields.io/badge/Layout-Root%20Scripts-informational)
![Legacy Scripts](https://img.shields.io/badge/Legacy%20Scripts-Present-orange)
![i18n](https://img.shields.io/badge/i18n-Enabled-brightgreen)
![Maintained Path](https://img.shields.io/badge/Video-v2c.py-2ea44f)

Python-инструментарий для генерации естественных текстовых описаний изображений и видео с помощью визуальных эмбеддингов OpenAI CLIP и языковой модели в стиле GPT.

## ✨ Обзор

Этот репозиторий предоставляет:

- Скрипты инференса для описания изображений и генерации субтитров для видео.
- Пайплайн обучения, который изучает отображение визуальных эмбеддингов CLIP в эмбеддинги токенов GPT-2.
- Утилиты подготовки датасета в стиле Flickr30k.
- Автоматическую загрузку checkpoint-файлов поддерживаемых размеров модели при отсутствии весов.
- Мультиязычные варианты README в `i18n/` (см. языковую строку выше).

Текущая реализация включает как новые, так и legacy-скрипты. Некоторые legacy-файлы сохранены для справки и задокументированы ниже.

## 🚀 Возможности

- Описание одного изображения через `image2caption.py`.
- Описание видео (равномерная выборка кадров) через `v2c.py` или `video2caption.py`.
- Настраиваемые runtime-параметры:
  - Количество кадров.
  - Размер модели.
  - Температура семплирования.
  - Имя checkpoint-файла.
- Многопроцессная/многопоточная генерация подписей для ускоренного инференса видео.
- Выходные артефакты:
  - Файлы субтитров SRT (`.srt`).
  - JSON-транскрипты (`.json`) в `v2c.py`.
- Точки входа для обучения и оценки экспериментов по отображению CLIP+GPT2.

### Кратко

| Область | Основной(ые) скрипт(ы) | Примечания |
|---|---|---|
| Описание изображений | `image2caption.py`, `i2c.py`, `predict.py` | CLI + переиспользуемый класс |
| Описание видео | `v2c.py` | Рекомендуемый поддерживаемый путь |
| Legacy-пайплайн видео | `video2caption.py`, `video2caption_v1.1.py` | Содержит machine-specific допущения |
| Сборка датасета | `dataset_generation.py` | Создает `data/processed/dataset.pkl` |
| Обучение / оценка | `training.py`, `evaluate.py` | Использует отображение CLIP+GPT2 |

## 🧱 Архитектура (высокоуровнево)

Базовая модель в `model/model.py` состоит из трех частей:

1. `ImageEncoder`: извлекает эмбеддинг изображения CLIP.
2. `Mapping`: проецирует эмбеддинг CLIP в последовательность prefix-эмбеддингов GPT.
3. `TextDecoder`: head языковой модели GPT-2, который авторегрессионно генерирует токены подписи.

Обучение (`Net.train_forward`) использует предвычисленные CLIP-эмбеддинги изображений + токенизированные подписи.
Инференс (`Net.forward`) использует PIL-изображение и декодирует токены до EOS или `max_len`.

### Поток данных

1. Подготовка датасета: `dataset_generation.py` читает `data/raw/results.csv` и изображения в `data/raw/flickr30k_images/`, записывает `data/processed/dataset.pkl`.
2. Обучение: `training.py` загружает сериализованные кортежи `(image_name, image_embedding, caption)` и обучает слои mapper/decoder.
3. Оценка: `evaluate.py` визуализирует сгенерированные подписи на отложенных тестовых изображениях.
4. Инференс:
   - image: `image2caption.py` / `predict.py` / `i2c.py`.
   - video: `v2c.py` (рекомендуется), `video2caption.py` (legacy).

## 🗂️ Структура проекта

```text
VideoCaptionerWithClip/
├── README.md
├── image2caption.py               # Single-image caption CLI
├── predict.py                     # Alternate single-image caption CLI
├── i2c.py                         # Reusable ImageCaptioner class + CLI
├── v2c.py                         # Video -> SRT + JSON (threaded frame captioning)
├── video2caption.py               # Alternate video -> SRT implementation (legacy constraints)
├── video2caption_v1.1.py          # Older variant
├── video2caption_v1.0_not_work.py # Explicitly marked non-working legacy file
├── training.py                    # Model training entrypoint
├── evaluate.py                    # Test-split evaluation and rendered outputs
├── dataset_generation.py          # Builds data/processed/dataset.pkl
├── data/
│   ├── __init__.py
│   └── dataset.py                 # Dataset + DataLoader helpers
├── model/
│   ├── __init__.py
│   ├── model.py                   # CLIP encoder + mapping + GPT2 decoder
│   └── trainer.py                 # Training/validation/test utility class
├── utils/
│   ├── __init__.py
│   ├── config.py                  # ConfigS / ConfigL defaults
│   ├── downloads.py               # Google Drive checkpoint downloader
│   └── lr_warmup.py               # LR warmup schedule
├── i18n/                          # Multilingual README variants
└── .auto-readme-work/             # Auto-README pipeline artifacts
```

## 📋 Предварительные требования

- Рекомендуется Python `3.10+`.
- GPU с поддержкой CUDA не обязателен, но настоятельно рекомендуется для обучения и инференса больших моделей.
- `ffmpeg` напрямую не требуется текущими скриптами (для извлечения кадров используется OpenCV).
- Интернет-доступ нужен при первом запуске для загрузки моделей/checkpoint-файлов из Hugging Face / Google Drive.

В проекте пока нет lockfile (`requirements.txt` / `pyproject.toml` отсутствуют), поэтому зависимости определяются по импортам.

## 🛠️ Установка

### Каноничная настройка для текущей структуры репозитория

```bash
git clone git@github.com:lachlanchen/VideoCaptionerWithClip.git
cd VideoCaptionerWithClip

python -m venv .venv
source .venv/bin/activate

pip install --upgrade pip
pip install torch torchvision torchaudio
pip install transformers pillow matplotlib numpy tqdm opencv-python pandas wandb gdown
```

### Фрагмент установки из исходного README (сохранен)

Предыдущий README обрывался на середине блока. Исходные команды сохранены ниже в точности как исторический первоисточник:

```bash
git clone git@github.com:lachlanchen/VideoCaptionerWithClip.git
cd VideoCaptionerWithClip/src
```

Примечание: в текущем состоянии репозитория скрипты расположены в корне репозитория, а не в `src/`.

## ▶️ Быстрый старт

### Описание изображения (быстрый запуск)

```bash
python image2caption.py -I /path/to/image.jpg -S L -C model.pt
```

### Описание видео (рекомендуемый путь)

```bash
python v2c.py -V /path/to/video.mp4 -N 10
```

## 🎯 Использование

### 1. Описание изображения (`image2caption.py`)

```bash
python image2caption.py \
  -I /path/to/image.jpg \
  -S L \
  -C model.pt \
  -R ./data/result/prediction \
  -T 1.0
```

Аргументы:

- `-I, --img-path`: путь к входному изображению.
- `-S, --size`: размер модели (`S` или `L`).
- `-C, --checkpoint-name`: имя checkpoint-файла в `weights/{small|large}`.
- `-R, --res-path`: выходная директория для изображения с отрисованной подписью.
- `-T, --temperature`: температура семплирования.

### 2. Альтернативный image CLI (`predict.py`)

```bash
python predict.py \
  -I /path/to/image.jpg \
  -S L \
  -C model.pt \
  -R ./data/result/prediction \
  -T 1.0
```

`predict.py` функционально похож на `image2caption.py`; форматирование выходного текста немного отличается.

### 3. API класса для описания изображения (`i2c.py`)

```bash
python i2c.py -I /path/to/image.jpg -S L -C model.pt -R ./data/result/prediction -T 1.0
```

Или импортируйте в собственный скрипт:

```python
from i2c import ImageCaptioner

captioner = ImageCaptioner(model_size="L", checkpoint_name="model.pt")
captioner.set_image_path("/path/to/image.jpg")
caption = captioner.generate_caption(save_image=True)
print(caption)
```

### 4. Видео в субтитры + JSON (`v2c.py`)

```bash
python v2c.py -V /path/to/video.mp4 -N 10
```

Выходные файлы рядом с входным видео:

- `<video_basename>_caption.srt`
- `<video_basename>_caption.json`
- `<video_basename>_captioning_frames/`

### 5. Альтернативный video-пайплайн (`video2caption.py`)

```bash
python video2caption.py -V /path/to/video.mp4 -N 10
```

Важно: этот скрипт сейчас содержит machine-specific hardcoded пути:

- Python path по умолчанию: `/home/lachlan/miniconda3/envs/caption/bin/python`
- Путь к скрипту описания: `/home/lachlan/Projects/image_captioning/clip-gpt-captioning/src/image2caption.py`

Используйте `v2c.py`, если только вы намеренно не поддерживаете эти пути.

### 6. Legacy-вариант (`video2caption_v1.1.py`)

Этот скрипт сохранен для исторической справки. Для активного использования предпочитайте `v2c.py`.

### 7. Генерация датасета

```bash
python dataset_generation.py
```

Ожидаемые сырые входы:

- `data/raw/results.csv` (таблица подписей с разделителем `|`).
- `data/raw/flickr30k_images/` (файлы изображений, на которые ссылается CSV).

Выход:

- `data/processed/dataset.pkl`

### 8. Обучение

```bash
python training.py -S L -C model.pt
```

Обучение по умолчанию использует логирование Weights & Biases (`wandb`).

### 9. Оценка

```bash
python evaluate.py \
  -I ./data/raw/flickr30k_images \
  -R ./data/result/eval \
  -S L \
  -C model.pt \
  -T 1.0
```

Оценка рендерит предсказанные подписи поверх тестовых изображений и сохраняет их в:

- `<res-path>/<checkpoint_name_without_ext>_<SIZE>/`

## ⚙️ Конфигурация

Конфигурации модели определены в `utils/config.py`:

| Config | CLIP backbone | GPT model | Weights dir |
|---|---|---|---|
| `ConfigS` | `openai/clip-vit-base-patch32` | `gpt2` | `weights/small` |
| `ConfigL` | `openai/clip-vit-large-patch14` | `gpt2-medium` | `weights/large` |

Ключевые значения по умолчанию из классов конфигурации:

| Field | `ConfigS` | `ConfigL` |
|---|---:|---:|
| `epochs` | 150 | 120 |
| `lr` | 3e-3 | 5e-3 |
| `batch_size_exp` | 6 | 5 |
| `ep_len` | 4 | 4 |
| `max_len` | 40 | 40 |

ID для авто-загрузки checkpoint-файлов находятся в `utils/downloads.py`:

| Size | Google Drive ID |
|---|---|
| `L` | `1Gh32arzhW06C1ZJyzcJSSfdJDi3RgWoG` |
| `S` | `1pSQruQyg8KJq6VmzhMLFbT_VaHJMdlWF` |

## 📦 Выходные файлы

### Инференс изображения

- Сохраненное изображение с наложенным/сгенерированным заголовком в `--res-path`.
- Шаблон имени файла: `<input_stem>-R<SIZE>.jpg`.

### Инференс видео (`v2c.py`)

- SRT: `<video_stem>_caption.srt`
- JSON: `<video_stem>_caption.json`
- Кадры: `<video_stem>_captioning_frames/`

Пример элемента JSON:

```json
{
  "start": "00:00:03,200",
  "end": "00:00:03,700",
  "lang": "en",
  "text": "A dog running through a field."
}
```

## 🧪 Примеры

### Быстрый пример описания изображения

```bash
python image2caption.py -I ./examples/dog.jpg -S S -C model.pt
```

Ожидаемое поведение:

- Если отсутствует `weights/small/model.pt`, файл будет загружен.
- Изображение с подписью по умолчанию записывается в `./data/result/prediction`.
- Текст подписи выводится в stdout.

### Быстрый пример описания видео

```bash
python v2c.py -V ./examples/demo.mp4 -N 8
```

Ожидаемое поведение:

- Будут описаны 8 равномерно выбранных кадров.
- Файлы `.srt` и `.json` будут сгенерированы рядом с входным видео.

### Последовательность обучения/оценки end-to-end

```bash
python dataset_generation.py
python training.py -S L -C model.pt
python evaluate.py -I ./data/raw/flickr30k_images -R ./data/result/eval -S L -C model.pt -T 1.0
```

## 🧭 Заметки по разработке

- В `v2c.py`, `video2caption.py` и `video2caption_v1.*` есть пересекающийся legacy-функционал.
- `video2caption_v1.0_not_work.py` намеренно сохранен как неработающий legacy-код.
- `training.py` сейчас выбирает `ConfigL()` через `config = ConfigL() if args.size.upper() else ConfigS()`, что всегда приводит к `ConfigL` при непустом значении `--size`.
- `model/trainer.py` использует `self.dataset` в `test_step`, тогда как в инициализаторе присваивается `self.test_dataset`; это может ломать выборку в обучающих запусках без исправления.
- `video2caption_v1.1.py` ссылается на `self.config.transform`, но в `ConfigS`/`ConfigL` поле `transform` не определено.
- В текущем снимке репозитория нет CI/набора тестов.
- Примечание по i18n: языковые ссылки есть вверху этого README; переводные файлы можно добавлять в `i18n/`.
- Примечание о текущем состоянии: языковая строка ссылается на `i18n/README.ru.md`, но в этом снимке этот файл отсутствует.

## 🩺 Устранение неполадок

- `AssertionError: Image does not exist`
  - Убедитесь, что `-I/--img-path` указывает на существующий файл.
- `Dataset file not found. Downloading...`
  - `MiniFlickrDataset` выбрасывает это сообщение, когда отсутствует `data/processed/dataset.pkl`; сначала выполните `python dataset_generation.py`.
- `Path to the test image folder does not exist`
  - Убедитесь, что `evaluate.py -I` указывает на существующую папку.
- Медленный или нестабильный первый запуск
  - При первом запуске загружаются модели Hugging Face и может выполняться загрузка checkpoint-файлов из Google Drive.
- `video2caption.py` возвращает пустые подписи
  - Проверьте hardcoded путь к скрипту и Python executable, либо переключитесь на `v2c.py`.
- Во время обучения `wandb` запрашивает вход
  - Выполните `wandb login` или при необходимости вручную отключите логирование в `training.py`.

## 🛣️ Дорожная карта

- Добавить lockfile зависимостей (`requirements.txt` или `pyproject.toml`) для воспроизводимой установки.
- Объединить дублирующиеся video-пайплайны в одну поддерживаемую реализацию.
- Убрать hardcoded machine paths из legacy-скриптов.
- Исправить известные edge-case баги обучения/оценки в `training.py` и `model/trainer.py`.
- Добавить автоматизированные тесты и CI.
- Заполнить `i18n/` переводами README, на которые ссылается языковая строка.

## 🤝 Вклад

Вклады приветствуются. Рекомендуемый рабочий процесс:

```bash
# 1) Fork and clone
git clone git@github.com:<your-user>/VideoCaptionerWithClip.git
cd VideoCaptionerWithClip

# 2) Create a feature branch
git checkout -b feat/your-change

# 3) Make changes and commit
git add .
git commit -m "feat: describe your change"

# 4) Push and open a PR
git push origin feat/your-change
```

Если вы меняете поведение модели, добавьте:

- Воспроизводимую(ые) команду(ы).
- Примеры результатов до/после.
- Примечания о предположениях по checkpoint-файлам или датасету.

## 🙌 Поддержка

В текущем снимке репозитория нет явной настройки донатов/спонсорства.

Если ссылки на спонсорство будут добавлены позже, их следует сохранить в этом разделе.

## 📄 Лицензия

В текущем снимке репозитория отсутствует файл лицензии.

Примечание-предположение: пока не добавлен файл `LICENSE`, условия повторного использования/распространения не определены.
