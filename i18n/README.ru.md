[English](../README.md) · [العربية](README.ar.md) · [Español](README.es.md) · [Français](README.fr.md) · [日本語](README.ja.md) · [한국어](README.ko.md) · [Tiếng Việt](README.vi.md) · [中文 (简体)](README.zh-Hans.md) · [中文（繁體）](README.zh-Hant.md) · [Deutsch](README.de.md) · [Русский](README.ru.md)


[![LazyingArt banner](https://github.com/lachlanchen/lachlanchen/raw/main/figs/banner.png)](https://github.com/lachlanchen/lachlanchen/blob/main/figs/banner.png)


# Clip-GPT-Captioning

![Python](https://img.shields.io/badge/Python-3.10%2B-blue)
![PyTorch](https://img.shields.io/badge/PyTorch-2.x-red)
![Transformers](https://img.shields.io/badge/HuggingFace-Transformers-yellow)
![Status](https://img.shields.io/badge/README-Expanded-success)
![Repo Layout](https://img.shields.io/badge/Layout-Root%20Scripts-informational)
![Legacy Scripts](https://img.shields.io/badge/Legacy%20Scripts-Present-orange)
![i18n](https://img.shields.io/badge/i18n-Enabled-brightgreen)
![Maintained Path](https://img.shields.io/badge/Video-v2c.py-2ea44f)

Python-инструмент для генерации естественных текстовых подписей для изображений и видео с помощью визуальных эмбеддингов OpenAI CLIP и языковой модели GPT-подобного типа.

## 🧭 Snapshot

| Параметр | Детали |
|---|---|
| Покрытие задач | Описание изображений и видео |
| Ключевые артефакты | SRT субтитры, JSON транскрипты, изображения с подписями |
| Основные скрипты | `i2c.py`, `v2c.py`, `image2caption.py` |
| Устаревшие пути | `video2caption.py` и версии файлов (сохраняются для истории) |
| Поток данных набора | `data/raw/results.csv` + `data/raw/flickr30k_images/` |

## ✨ Обзор

Этот репозиторий предоставляет:

- Скрипты инференса для описания изображений и генерации видео-субтитров.
- Пайплайн обучения, который выучивает отображение от эмбеддингов CLIP к эмбеддингам токенов GPT-2.
- Утилиты генерации датасета в стиле Flickr30k.
- Автоматическую загрузку контрольных точек (checkpoints) для поддерживаемых размеров моделей при отсутствии весов.
- Мультиязычные варианты README в `i18n/` (см. языковую панель выше).

Текущая реализация включает как новые, так и legacy-скрипты. Некоторые файлы legacy сохранены для справки и задокументированы ниже.

## 🚀 Функции

- Описание одного изображения через `image2caption.py`.
- Описание видео (равномерная выборка кадров) через `v2c.py` или `video2caption.py`.
- Настраиваемые параметры выполнения:
  - Число кадров.
  - Размер модели.
  - Температура сэмплирования.
  - Имя checkpoint-файла.
- Многопроцессное/многопоточное описание для ускорения инференса видео.
- Выходные артефакты:
  - SRT-файлы субтитров (`.srt`).
  - JSON-транскрипты (`.json`) в `v2c.py`.
- Точки входа для обучения и оценки экспериментов с отображением CLIP+GPT2.

### В двух словах

| Область | Основной(ые) скрипт(ы) | Примечания |
|---|---|---|
| Описание изображений | `image2caption.py`, `i2c.py`, `predict.py` | CLI + переиспользуемый класс |
| Описание видео | `v2c.py` | Рекомендуемый поддерживаемый путь |
| Legacy-поток видео | `video2caption.py`, `video2caption_v1.1.py` | Содержит предположения, специфичные для машины |
| Построение датасета | `dataset_generation.py` | Формирует `data/processed/dataset.pkl` |
| Обучение / оценка | `training.py`, `evaluate.py` | Использует отображение CLIP+GPT2 |

## 🧱 Архитектура (высокоуровнево)

Ядро модели в `model/model.py` состоит из трех частей:

1. `ImageEncoder`: извлекает эмбеддинг изображения CLIP.
2. `Mapping`: проецирует эмбеддинг CLIP в последовательность prefix-эмбеддингов GPT.
3. `TextDecoder`: голова языковой модели GPT-2, которая авто-регрессионно генерирует токены подписи.

Обучение (`Net.train_forward`) использует предварительно вычисленные CLIP-эмбеддинги изображений и токенизированные подписи.
Инференс (`Net.forward`) использует изображение PIL и декодирует токены до EOS или `max_len`.

### Поток данных

1. Подготовка датасета: `dataset_generation.py` читает `data/raw/results.csv` и изображения в `data/raw/flickr30k_images/`, записывает `data/processed/dataset.pkl`.
2. Обучение: `training.py` загружает сериализованные кортежи `(image_name, image_embedding, caption)` и обучает слои mapper/decoder.
3. Оценка: `evaluate.py` генерирует подписи для отложенных тестовых изображений.
4. Выполнение инференса:
   - изображение: `image2caption.py` / `predict.py` / `i2c.py`.
   - видео: `v2c.py` (рекомендуется), `video2caption.py` (legacy).

## 🗂️ Структура проекта

```text
VideoCaptionerWithClip/
├── README.md
├── image2caption.py               # Single-image caption CLI
├── predict.py                     # Alternate single-image caption CLI
├── i2c.py                         # Reusable ImageCaptioner class + CLI
├── v2c.py                         # Video -> SRT + JSON (threaded frame captioning)
├── video2caption.py               # Alternate video -> SRT implementation (legacy constraints)
├── video2caption_v1.1.py          # Older variant
├── video2caption_v1.0_not_work.py # Explicitly marked non-working legacy file
├── training.py                    # Model training entrypoint
├── evaluate.py                    # Test-split evaluation and rendered outputs
├── dataset_generation.py          # Builds data/processed/dataset.pkl
├── data/
│   ├── __init__.py
│   └── dataset.py                 # Dataset + DataLoader helpers
├── model/
│   ├── __init__.py
│   ├── model.py                   # CLIP encoder + mapping + GPT2 decoder
│   └── trainer.py                 # Training/validation/test utility class
├── utils/
│   ├── __init__.py
│   ├── config.py                  # ConfigS / ConfigL defaults
│   ├── downloads.py               # Google Drive checkpoint downloader
│   └── lr_warmup.py               # LR warmup schedule
├── i18n/                          # Multilingual README variants
└── .auto-readme-work/             # Auto-README pipeline artifacts
```

## 📋 Предварительные требования

- Рекомендуется Python `3.10+`.
- Для обучения и инференса больших моделей CUDA-совместимая GPU не обязательна, но настоятельно рекомендуется.
- `ffmpeg` не используется напрямую текущими скриптами (для извлечения кадров применяется OpenCV).
- Для первого скачивания моделей/checkpoint-ов с Hugging Face / Google Drive нужен доступ в интернет.

На данный момент lockfile отсутствует (`requirements.txt` / `pyproject.toml` нет), поэтому зависимости восстановлены по импортам.

## 🛠️ Установка

### Каноническая установка из текущей структуры репозитория

```bash

git clone git@github.com:lachlanchen/VideoCaptionerWithClip.git
cd VideoCaptionerWithClip

python -m venv .venv
source .venv/bin/activate

pip install --upgrade pip
pip install torch torchvision torchaudio
pip install transformers pillow matplotlib numpy tqdm opencv-python pandas wandb gdown
```

### Оригинальный фрагмент установки из README (сохранен)

Предыдущий README обрывался в середине блока. Исходные команды сохранены ниже в точности как исторически корректное содержимое:

```bash
git clone git@github.com:lachlanchen/VideoCaptionerWithClip.git
cd VideoCaptionerWithClip/src
```

Примечание: текущий снимок репозитория располагает скрипты в корне репозитория, а не в `src/`.

## ▶️ Quick Start

| Цель | Команда |
|---|---|
| Сгенерировать подпись для изображения | `python image2caption.py -I /path/to/image.jpg -S L -C model.pt` |
| Сгенерировать подпись для видео | `python v2c.py -V /path/to/video.mp4 -N 10` |
| Построить датасет | `python dataset_generation.py` |

### Описание изображения (быстрый запуск)

```bash
python image2caption.py -I /path/to/image.jpg -S L -C model.pt
```

### Описание видео (рекомендуемый путь)

```bash
python v2c.py -V /path/to/video.mp4 -N 10
```

## 🎯 Использование

### 1. Описание изображения (`image2caption.py`)

```bash
python image2caption.py \
  -I /path/to/image.jpg \
  -S L \
  -C model.pt \
  -R ./data/result/prediction \
  -T 1.0
```

Аргументы:

- `-I, --img-path`: путь к входному изображению.
- `-S, --size`: размер модели (`S` или `L`).
- `-C, --checkpoint-name`: имя checkpoint-файла в `weights/{small|large}`.
- `-R, --res-path`: выходной каталог для изображения с отрисованной подписью.
- `-T, --temperature`: температура сэмплирования.

### 2. Альтернативный CLI для изображений (`predict.py`)

```bash
python predict.py \
  -I /path/to/image.jpg \
  -S L \
  -C model.pt \
  -R ./data/result/prediction \
  -T 1.0
```

`predict.py` функционально близок к `image2caption.py`; формат вывода текста немного отличается.

### 3. API класса для описания изображений (`i2c.py`)

```bash
python i2c.py -I /path/to/image.jpg -S L -C model.pt -R ./data/result/prediction -T 1.0
```

Или импортируйте в собственный скрипт:

```python
from i2c import ImageCaptioner

captioner = ImageCaptioner(model_size="L", checkpoint_name="model.pt")
captioner.set_image_path("/path/to/image.jpg")
caption = captioner.generate_caption(save_image=True)
print(caption)
```

### 4. Видео в субтитры + JSON (`v2c.py`)

```bash
python v2c.py -V /path/to/video.mp4 -N 10
```

Выходные данные создаются рядом с исходным видео:

- `<video_basename>_caption.srt`
- `<video_basename>_caption.json`
- `<video_basename>_captioning_frames/`

### 5. Альтернативный видео-пайплайн (`video2caption.py`)

```bash
python video2caption.py -V /path/to/video.mp4 -N 10
```

Важно: этот скрипт сейчас содержит жестко заданные пути, специфичные для конкретной машины:

- Python path по умолчанию: `/home/lachlan/miniconda3/envs/caption/bin/python`
- Путь к скрипту описания: `/home/lachlan/Projects/image_captioning/clip-gpt-captioning/src/image2caption.py`

Используйте `v2c.py`, если только вы намеренно не поддерживаете эти пути.

### 6. Legacy-вариант (`video2caption_v1.1.py`)

Этот скрипт сохраняется для исторической справки. Для активного использования предпочитайте `v2c.py`.

### 7. Генерация датасета

```bash
python dataset_generation.py
```

Ожидаемые входные сырые данные:

- `data/raw/results.csv` (таблица подписей с разделителем `|`).
- `data/raw/flickr30k_images/` (файлы изображений, на которые ссылается CSV).

Вывод:

- `data/processed/dataset.pkl`

### 8. Обучение

```bash
python training.py -S L -C model.pt
```

Обучение по умолчанию использует логирование Weights & Biases (`wandb`).

### 9. Оценка

```bash
python evaluate.py \
  -I ./data/raw/flickr30k_images \
  -R ./data/result/eval \
  -S L \
  -C model.pt \
  -T 1.0
```

Оценка рендерит предсказанные подписи на отложенных тестовых изображениях и сохраняет их в:

- `<res-path>/<checkpoint_name_without_ext>_<SIZE>/`

## ⚙️ Конфигурация

Конфигурация модели определена в `utils/config.py`:

| Конфиг | CLIP backbone | Модель GPT | Каталог весов |
|---|---|---|---|
| `ConfigS` | `openai/clip-vit-base-patch32` | `gpt2` | `weights/small` |
| `ConfigL` | `openai/clip-vit-large-patch14` | `gpt2-medium` | `weights/large` |

Ключевые значения по умолчанию из классов конфигурации:

| Поле | `ConfigS` | `ConfigL` |
|---|---:|---:|
| `epochs` | 150 | 120 |
| `lr` | 3e-3 | 5e-3 |
| `batch_size_exp` | 6 | 5 |
| `ep_len` | 4 | 4 |
| `max_len` | 40 | 40 |

Идентификаторы Google Drive для авто-загрузки checkpoint есть в `utils/downloads.py`:

| Размер | Google Drive ID |
|---|---|
| `L` | `1Gh32arzhW06C1ZJyzcJSSfdJDi3RgWoG` |
| `S` | `1pSQruQyg8KJq6VmzhMLFbT_VaHJMdlWF` |

## 📦 Выходные файлы

### Инференс изображения

- Сохраненное изображение с наложенным/сгенерированным заголовком по `--res-path`.
- Шаблон имени файла: `<input_stem>-R<SIZE>.jpg`.

### Инференс видео (`v2c.py`)

- SRT: `<video_stem>_caption.srt`
- JSON: `<video_stem>_caption.json`
- Кадровые изображения: `<video_stem>_captioning_frames/`

Пример элемента JSON:

```json
{
  "start": "00:00:03,200",
  "end": "00:00:03,700",
  "lang": "en",
  "text": "A dog running through a field."
}
```

## 🧪 Примеры

### Быстрый пример описания изображения

```bash
python image2caption.py -I ./examples/dog.jpg -S S -C model.pt
```

Ожидаемое поведение:

- Если отсутствует `weights/small/model.pt`, он будет загружен.
- Изображение с подписью по умолчанию записывается в `./data/result/prediction`.
- Текст подписи выводится в stdout.

### Быстрый пример описания видео

```bash
python v2c.py -V ./examples/demo.mp4 -N 8
```

Ожидаемое поведение:

- Описывается 8 равномерно выбранных кадров.
- Файлы `.srt` и `.json` создаются рядом с исходным видео.

### Сквозная последовательность обучения/оценки

```bash
python dataset_generation.py
python training.py -S L -C model.pt
python evaluate.py -I ./data/raw/flickr30k_images -R ./data/result/eval -S L -C model.pt -T 1.0
```

## 🧭 Заметки по разработке

- Имеется пересечение legacy-функционала между `v2c.py`, `video2caption.py` и `video2caption_v1.*`.
- `video2caption_v1.0_not_work.py` намеренно оставлен как нерабочий legacy-код.
- `training.py` сейчас выбирает `ConfigL()` через `config = ConfigL() if args.size.upper() else ConfigS()`, что приводит к `ConfigL` для всех непустых значений `--size`.
- `model/trainer.py` использует `self.dataset` в `test_step`, в то время как в инициализаторе присваивается `self.test_dataset`; это может ломать выборку в обучении, если не поправить.
- `video2caption_v1.1.py` ссылается на `self.config.transform`, но `ConfigS`/`ConfigL` не определяют `transform`.
- В текущем снимке репозитория не определен CI/набор тестов.
- Примечание по i18n: языковые ссылки есть в верхней части этого README; переведенные файлы могут быть добавлены в `i18n/`.
- Текущее состояние: языковая панель указывает на `i18n/README.ru.md`, но в этом снимке файл отсутствует.

## 🩺 Устранение неполадок

- `AssertionError: Image does not exist`
  - Убедитесь, что `-I/--img-path` указывает на существующий файл.
- `Dataset file not found. Downloading...`
  - `MiniFlickrDataset` выбрасывает это сообщение, если отсутствует `data/processed/dataset.pkl`; сначала запустите `python dataset_generation.py`.
- `Path to the test image folder does not exist`
  - Убедитесь, что `evaluate.py -I` указывает на существующий каталог.
- Медленный или падающий первый запуск
  - На первом прогоне загружаются модели Hugging Face и могут загружаться checkpoints с Google Drive.
- `video2caption.py` возвращает пустые подписи
  - Проверьте жёстко прописанные пути к скрипту и Python executable, или переключитесь на `v2c.py`.
- `wandb` запрашивает вход во время обучения
  - Выполните `wandb login` или отключите логирование вручную в `training.py` при необходимости.

## 🛣️ Дорожная карта

- Добавить lockfile зависимостей (`requirements.txt` или `pyproject.toml`) для воспроизводимости установки.
- Объединить дублирующиеся видео-пайплайны в одну поддерживаемую реализацию.
- Удалить жестко заданные машинные пути из legacy-скриптов.
- Исправить известные краевые ошибки обучения/оценки в `training.py` и `model/trainer.py`.
- Добавить автоматические тесты и CI.
- Заполнить `i18n/` переведенными README из языковой панели.

## 🤝 Вклад

Вклад приветствуется. Предлагаемый рабочий процесс:

```bash
# 1) Fork and clone
git clone git@github.com:<your-user>/VideoCaptionerWithClip.git
cd VideoCaptionerWithClip

# 2) Create a feature branch
git checkout -b feat/your-change

# 3) Make changes and commit
git add .
git commit -m "feat: describe your change"

# 4) Push and open a PR
git push origin feat/your-change
```

Если вы меняете поведение модели, добавьте:

- Воспроизводимую(ые) команду(ы).
- Примеры вывода до/после.
- Примечания по предпосылкам для checkpoint или датасета.

## ❤️ Support

| Donate | PayPal | Stripe |
|---|---|---|
| [![Donate](https://img.shields.io/badge/Donate-LazyingArt-0EA5E9?style=for-the-badge&logo=ko-fi&logoColor=white)](https://chat.lazying.art/donate) | [![PayPal](https://img.shields.io/badge/PayPal-RongzhouChen-00457C?style=for-the-badge&logo=paypal&logoColor=white)](https://paypal.me/RongzhouChen) | [![Stripe](https://img.shields.io/badge/Stripe-Donate-635BFF?style=for-the-badge&logo=stripe&logoColor=white)](https://buy.stripe.com/aFadR8gIaflgfQV6T4fw400) |

## 📄 Лицензия

В текущем снимке репозитория отсутствует файл лицензии.

Примечание: пока файл `LICENSE` не добавлен, условия повторного использования/распространения не определены.
